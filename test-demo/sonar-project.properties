# Required metadata
sonar.projectKey=b2bop.xgfe.admin
sonar.projectName=xgfe.admin

sonar.projectVersion=v3.6.8.0
# optional description
sonar.projectDescription=b2bop.xgfe.admin

# Comma-separated paths to directories with sources (required)
sonar.sources=src/app

# exclude directories，该字段会覆盖掉 sonar 平台上设置的字段，所以通过修改sonar.sources、注释掉了 sonar.exclusions
# sonar.exclusions=src/lib/**

# Language
sonar.language=js

# Encoding of sources files
sonar.sourceEncoding=UTF-8
